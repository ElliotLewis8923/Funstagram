
	<%= bootstrap_form_for([@post, @comment], remote: true, class: '.form-control, modalForm') do |f| %>
	<%= f.text_area :text, :placeholder => "Leave a comment" %>
	<%= f.submit 'Submit' %>
	<% end %>
